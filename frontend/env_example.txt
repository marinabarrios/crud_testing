# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# App Configuration
NEXT_PUBLIC_APP_NAME=Tienda CRUD
NEXT_PUBLIC_APP_VERSION=1.0.0

# INSTRUCCIONES:
# 1. Copia este archivo como .env en la carpeta frontend/
# 2. Ejecuta: cp env_example.txt .env
# 3. Asegúrate de que el backend esté corriendo en localhost:8000